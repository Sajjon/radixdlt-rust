//use crate::atom_model::{EUID};
//use crate::address::{Address};
//use crate::serialization::{DSONSerializable};

pub trait Particle: Send + Sync {
//    fn destinations(&self) -> HashSet<EUID>;
//    fn shardables(&self) -> HashSet<Address>;
}

